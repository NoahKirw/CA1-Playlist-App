<h2>{{playlist.title}}</h2>

{{#each playlist.songs}}
  <div class="song-item">
    <strong>{{title}}</strong> - {{artist}}
    <div>Duration: {{duration}}</div>
  </div>
{{/each}}

<a href="/dashboard" class="ui button primary back-button">
  Back to Dashboard
</a>